version: '3.8'

services:
  portfolio:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped

  portfolio-prod:
    build: .
    restart: unless-stopped